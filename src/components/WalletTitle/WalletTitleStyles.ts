import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  text: {
    fontFamily: 'PTSans-Regular',
    fontSize: 28,
    lineHeight: 32,
    color: '#ffffff',
    marginBottom: 32,
  },
});
